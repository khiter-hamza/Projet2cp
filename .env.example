# App Settings
PROJECT_NAME="Projet 2CP API"
VERSION="1.0.0"
API_V1_STR="/api/v1"

# Security
# Run 'openssl rand -hex 32' to generate a real one
SECRET_KEY=change-me-to-a-secure-key
ACCESS_TOKEN_EXPIRE_MINUTES=11520

# Database
POSTGRES_SERVER=db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=projet2cp
POSTGRES_PORT=5432

# CORS
CORS_ORIGINS=["http://localhost:3000"]
